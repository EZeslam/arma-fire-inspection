# Project Primer — Imported by Stunning
This codebase was imported via Stunning's GitHub import. The AI assistant uses this primer to ramp up on the project structure before making non-trivial edits.
**Stack:** Static HTML
## Available scripts
(none)
## Top-level files (up to 40)
- `]654;interactive]654;interactive]654;interactive./.gitignore`
- `.stunning/run.json`
- `index.html`
## Notes for the AI assistant
- This is an existing codebase, not a freshly generated template. Match the conventions you find.
- Use `glob` + `grep` to navigate before making non-trivial edits.
- The user's backend (DB, APIs, env vars) lives outside this repo — don't try to provision or migrate it.
- `.stunning/run.json` records how the dev server is started; leave it alone.